# 表格重复 style 属性修复（2025-11-20 23:26）

## 问题
- `frontend/src/views/files/Explorer.vue` 的 `<el-table>` 同时存在两个 `style` 属性，Vue 编译报 “Duplicate attribute”，导致构建失败。

## 修复
- 合并表格的样式声明，将宽度与 table-layout 写入同一个 `style` 属性，保留 `:fit="false"`。

## 影响
- 仅修复编译错误，不影响功能逻辑与样式效果。建议重新构建验证通过。
