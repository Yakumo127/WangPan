# 存储卡片刷新按钮选中文本问题修复

## 问题
- 系统设置「存储与空间」页，总文件占用卡片的“刷新”按钮在多次点击后会触发页面文字（如“垃圾分片占用”）被选中，影响体验。

## 成因
- 浏览器默认的 mousedown 行为在连续点击时可能触发文本选择，按钮未阻止默认行为。

## 修复方案
- 在卡片按钮（刷新、清理）上添加 `@mousedown.prevent`，阻止默认选择行为，避免多次点击导致文字选中。

## 涉及文件
- `frontend/src/views/admin/System.vue`

## 结果
- 多次点击按钮不再选中文本，行为保持一致。
