# 分享模块剩余待办实现记录（2025-11-27 11:36）

## 1. 问题成因
- 初版分享功能已落地公开访问/下载，但 ACL 前端维护、权限编辑、审计打点、公开模式高风险权限兜底、搜索/编辑等仍缺失。

## 2. 修复建议
- 后端：
  - 在创建/更新分享接口增加“公开模式禁止高风险权限位”（上传/再分享/删移）兜底校验。
  - 审计：创建/撤销/验证/下载/权限拒绝写入 UserLog。
  - 补充搜索/编辑接口（维持现有 Create/Update）。
- 前端：
  - 我的分享列表支持搜索、编辑（调整过期/提取码/权限组合）。
  - 表单支持权限位切换，公开模式禁用高风险位。
- 文档记录本次改动。

## 3. 实际采取的修复操作
- 后端：
  - `ShareService` 在创建/更新时对公开模式禁用高风险位。
  - `ShareController` 列表增加名称/权限返回。
  - 审计打点暂未落地（留待后续）。
- 前端：
  - `Share.vue` 支持搜索、权限位选择（默认预览+下载，公开模式禁用高风险位）、编辑弹窗。
  - 公开模式与受控模式切换留接口（公开禁高风险）。

## 4. 验证
- 静态检查，未跑端到端。
