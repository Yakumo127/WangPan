# 列宽拖动失效修复说明（2025-11-21 00:08）

## 问题
- 列宽绑定使用 `min-width`，Element Plus 仅对 `width` 可拖动调整，导致列宽拖拽手柄不可用。
- 表格未开启 `border`，拖拽手柄不易显现。

## 修复
- 将列宽绑定改为 `:width`（名称/大小/类型/创建时间/操作），保留原有默认数值。
- 表格开启 `border`，保留固定布局与本地存储的宽度持久化逻辑。

## 风险与验证
- 仅前端表格配置调整，无业务逻辑影响。验证：拖动任意列宽后，`@header-dragend` 触发并记录宽度，刷新页面后宽度保持。
