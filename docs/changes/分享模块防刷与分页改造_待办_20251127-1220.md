# 分享模块防刷与分页改造待办（2025-11-27 12:20）

## 1. 待办事项
- 下载限速：公开直链下载增加后端限速（默认 3MB/s），并在系统设置中支持修改；提供禁用/开关配置。
- 分页改造：
  - 我的分享列表：后端支持 page/size/sort（按创建时间），返回 total+items；前端接入分页。
  - 公开分享列表：后端分页返回 total+items，按创建时间排序；前端分页导航接入。
- 受控模式 ACL：确认是否仅保留 USER 维度（当前倾向隐藏 EMAIL/GROUP）。

## 2. 现状
- 公开链接已支持提取码、防爆破（4次失败封禁），但未限速。
- 公开列表当前为内存分页，前端分页已加，但后端未真正分页查询。
- shareMode 已有 PUBLIC/CONTROLLED，ACL 管理 UI 已可添加 USER/EMAIL/组，EMAIL/组未验证。

## 3. 建议实现方案
- 下载限速：在 `direct-download` 输出流增加速率限制器（令牌桶/睡眠节奏），默认 3MB/s；通过系统设置表/配置接口可调整或关闭。
- 分页：新增分页参数到分享列表接口（我的分享/公开列表），Repository/Service 支持 PageRequest 排序，前端表格接入分页控件。
- 受控 ACL：若不启用 EMAIL/GROUP 验证，前端隐藏或标注仅 USER 生效。

## 4. 待确认
- 是否隐藏 EMAIL/GROUP ACL 选项，仅保留 USER。
- 系统设置存储限速的具体字段与接口（新建或复用设置表）。+默认限速/开关的口径。
